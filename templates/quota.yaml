apiVersion: v1
kind: ResourceQuota
metadata: 
  name: capstone-quota
spec:
  hard:
    requests.cpu : {{.Values.quota.requestsCpu}}
    requests.memory: {{.Values.quota.requestsMemory}}
    limits.cpu: {{.Values.quota.limitCpu}}
    limits.memory: {{.Values.quota.limitMemory}}
    pods : {{.Values.quota.maxPods}}
